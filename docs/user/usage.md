# Review usage

The Usage page combines Codex and Claude Code activity from your connected environments. It reads
the providers' local session history and shows API-equivalent token cost, processed tokens, cache
savings, provider shares, and model breakdowns. Subscription billing is separate from the raw token
cost shown here.

Use **Past 24h** for an hourly chart covering the exact rolling 24-hour period. The **7 days**,
**30 days**, and **90 days** ranges use daily resolution. Cost and token toggles update both the
headline and chart, and refreshing rescans every connected environment.

The message composer also shows a compact weekly allowance when the selected provider reports one.
Open it to see the reset time and any model-specific weekly limits. This provider-reported percentage
is separate from the token and API-equivalent cost history on the Usage page, and it appears only
after the provider has made allowance data available to the current environment.
